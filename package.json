{
  "name": "em-projects-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for EM Projects and DiveIQ (served via minimal Node.js on Railway)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "serve . --listen tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
